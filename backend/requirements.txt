# Lead Generation Platform - Backend Requirements

# FastAPI and server
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.0
python-multipart==0.0.9

# Auth & Security
pyjwt==2.9.0
bcrypt==4.2.0

# HTTP client
httpx==0.27.1
requests>=2.32.3

# Data processing
pandas==2.2.0

# Python standard library (no install needed):
# - sqlite3
# - asyncio
# - csv
# - json
# - logging
# - pathlib
# - threading
# - uuid

# MCP server (Phase 1A — added 2026-07-16)
# Pins: sse-starlette must stay at 1.6.1 to avoid pulling starlette 1.x (incompatible with FastAPI 0.115)
# Pins: httpx must stay at 0.27 to avoid breaking changes in 0.28
mcp==1.28.1
sse-starlette==1.6.1
